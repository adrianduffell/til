# Disable "swipe between pages" gesture.

I sometimes lose work-in-progress when using Safari and a trackpad because I trigger the "swipe between pages" guesture accidentally.

This can be disabled under System Settings > Trackpad > More Gestures > Swipe between pages (Off)
